defaults: &defaults
  host: mongo
  port: 27017

development:
  <<: *defaults
  database: places_phl

test:
  <<: *defaults
  database: places_phl_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: places_phl
